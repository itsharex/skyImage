version: '3.8'

services:
  skyimage:
    image: fishcpy/skyimage:latest
    container_name: skyimage
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - HTTP_ADDR=:8080
      - STORAGE_PATH=storage/uploads
      - PUBLIC_BASE_URL=http://localhost:8080
      - ALLOW_REGISTRATION=true
      - FRONTEND_DIST=dist
      - TZ=Asia/Shanghai
      # CORS 配置（生产环境请修改为实际域名）
      - CORS_ALLOWED_ORIGINS=http://localhost:8080
    volumes:
      - ./storage/data:/app/storage/data
      - ./storage/uploads:/app/storage/uploads
      # 重要：.env 文件持久化，保存安装后的数据库配置。
      # 首次启动前必须确保宿主机存在 .env 文件（touch .env 或 cp .env.example .env），
      # 否则 Docker 会把 ./.env 创建为目录，应用将无法保存数据库配置。
      - ./.env:/app/.env
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s